# ToDo_Application
Created a RESTful API for a To-Do Manager application using Spring Boot and MySQL. The project supports creating, updating, deleting, and retrieving tasks, and is structured following best practices for a scalable and maintainable backend system. It uses JPA for ORM and includes proper error handling and layered architecture.
